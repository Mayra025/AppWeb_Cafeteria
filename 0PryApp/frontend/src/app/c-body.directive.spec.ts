import { CBodyDirective } from './c-body.directive';

describe('CBodyDirective', () => {
  it('should create an instance', () => {
    const directive = new CBodyDirective();
    expect(directive).toBeTruthy();
  });
});
